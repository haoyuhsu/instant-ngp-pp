root_dir = ../datasets/lerf/dozer_nerfgun_waldo
dataset_name = lerf
exp_name = dozer_nerfgun_waldo
batch_size = 2048
scale = 8.0
num_epochs = 40

### render a camera path(through interpolation between poses)
render_traj = True 

### render camera poses from training dataset
render_train = False

render_rgb = True
render_depth = True

### render derived normal or not
render_normal = True

### render semantic labels or not, set to False if no g.t. semantic labels
# render_semantic = False
# num_classes = 6    # for category case (5 category)

### appearance embeddings
embed_a = True
embed_a_len = 8

### mask embeddings
embed_msk = True

random_bg = True

### mesh extraction
grid_dim = "512 512 128"
min_bound = "-1. -1. -0.3"
max_bound = "1. 1. 0.15"